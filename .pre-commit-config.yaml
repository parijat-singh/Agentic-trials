# Optional: Run `pip install pre-commit && pre-commit install` to run tests on every commit
repos:
  - repo: local
    hooks:
      - id: pytest
        name: Run unit tests
        entry: pytest tests/ -v --tb=short -x
        language: system
        pass_filenames: false
